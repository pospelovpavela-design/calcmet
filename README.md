# MetalCalc Suite — Расчёт металлоёмкости производственных зданий

Десктопное приложение для определения металлоёмкости одноэтажных производственных зданий с мостовыми кранами, трубопроводных и электрокабельных эстакад.

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![CustomTkinter](https://img.shields.io/badge/UI-CustomTkinter-informational)
![License](https://img.shields.io/badge/license-MIT-green)

---

## Содержание

- [Структура проекта](#структура-проекта)
- [Возможности](#возможности)
- [Установка и запуск](#установка-и-запуск)
- [Параметры расчёта](#параметры-расчёта)
- [Кровельный пирог](#кровельный-пирог)
- [Методика расчёта](#методика-расчёта)
- [Эстакады](#эстакады)
- [Тесты](#тесты)
- [Сборка в исполняемый файл](#сборка-в-исполняемый-файл)
- [База знаний](KNOWLEDGE_BASE.md)
- [План верификации расчётов](VERIFICATION_PLAN.md)

---

## Структура проекта

```
metalloemkost_calculator/
├── launcher.py          # Единый лаунчер — открывает нужный калькулятор
├── main_desktop.py      # Производственные здания: вся логика + GUI (v3.0)
├── estakada_pipe.py     # Трубопроводные эстакады (v3.2F)
├── estakada_elec.py     # Электрокабельные эстакады и галереи (v4.2F)
├── calculator_logic.py  # Вспомогательные расчётные функции
├── requirements.txt
├── docs/
│   └── BUILD.md         # Инструкция по сборке DMG/EXE
└── tests/
    ├── conftest.py           # Заглушки customtkinter/tkinter для pytest
    ├── test_main_desktop.py  # ~226 тестов: pure functions + calculate()
    └── test_estakada.py      # Тесты CONFIGS трубопроводных и электроэстакад
```

### Точки входа

| Файл | Назначение |
|---|---|
| `launcher.py` | Главный лаунчер — выбор калькулятора |
| `main_desktop.py` | Запуск напрямую: `python main_desktop.py` |
| `estakada_pipe.py` | Запуск напрямую |
| `estakada_elec.py` | Запуск напрямую |

---

## Возможности

### Производственные здания (`main_desktop.py`)

- **Многопролётные здания** (до 6 пролётов) с независимыми параметрами каждого пролёта
- **Два метода расчёта:**
  - **Метод 1** — аналитические формулы (полностью встроен, внешние файлы не нужны)
  - **Метод 2** — табличные данные из xlsx/docx файлов (если файлы не найдены — используются встроенные значения)
- **Кровельный пирог** — конструктор из ~30 слоёв (утеплители, настилы, гидро- и пароизоляция) с авторасчётом нагрузки кН/м² и 4 предустановленными типами
- **Режим работы крана:** 1К–6К (лёгкий/средний) и 7К–8К (тяжёлый) с корректными коэффициентами для каждого метода
- **Прогоны:** отдельный шаг прогонов задаётся в интерфейсе; профиль подбирается по Таблице 3 методики
- **Экспорт результатов** в текстовый файл
- **Всплывающие подсказки** (`?`) по каждому параметру со ссылками на нормативы

### Трубопроводные эстакады (`estakada_pipe.py`, v3.2F)

- 9 типоразмеров от 1 до 9 трубопроводов
- Ввод длины эстакады; вывод: масса металлоконструкций (т), масса на м.п.

### Электрокабельные эстакады и галереи (`estakada_elec.py`, v4.2F)

- Эстакады без прохода и галереи с проходом
- Несколько типоразмеров по ширине и нагрузке

---

## Установка и запуск

```bash
pip install -r requirements.txt
python launcher.py
```

**Зависимости:** `customtkinter >= 5.2`, `pandas >= 2.0`, `openpyxl >= 3.1`, `python-docx >= 1.1`

### Файлы таблиц (Метод 2)

Программа ищет файлы в следующем порядке:
1. Папка рядом с `main_desktop.py`
2. `~/Desktop/Металлоемкость/Тип 1 здания с кранами/`

| Файл | Содержимое |
|---|---|
| `металлоекмсоть покрытия.xlsx` | Фермы (Уголки/Двутавры/Молодечно), подстропильные, связи |
| `Металлоёмкость фахверк.xlsx` | Фахверк по схемам I/II/III, опоры трубопроводов |
| `Таблица металлоемкости на подкрановые конструкции.docx` | Подкрановые балки по г/п и шагу |
| `Таблица металлоемкости на тормозные конструкции.docx` | Тормозные конструкции |

Если файлы не найдены — программа работает полностью в режиме Метод 1.

---

## Параметры расчёта

### Общие параметры

| Параметр | Описание |
|---|---|
| Длина здания | Продольный размер, м (кратно шагу колонн) |
| Кол-во пролётов | 1–6; каждый пролёт задаётся независимо |

### Параметры пролёта

| Параметр | Описание | Норматив |
|---|---|---|
| L (пролёт) | Расстояние между рядами колонн, м | ГОСТ 23837 |
| Шаг ферм B | 6 или 12 м | — |
| Шаг колонн | 6 или 12 м; при 12 м — подстропильные фермы | — |
| Тип фермы | Уголки / Двутавры / Молодечно | — |
| Г/П крана | Грузоподъёмность, т | Техзадание на кран |
| Кол-во кранов | 1 или 2 | — |
| Тормозные | С проходом / Без прохода | — |
| Режим крана | 1К–6К / 7К–8К | ГОСТ 25546 |
| УГР | Отметка головки рельса от ±0.000, м | Техзадание на кран |
| H_кол | Полная высота колонны, м (0 = автоматически) | — |
| Снег | Нормативная нагрузка, кН/м² | СП 20.13330.2017, Прил. Ж |
| Пыль | Технологическая нагрузка, кН/м² | Техзадание |
| Кровля | Нагрузка от кровельного пирога, кН/м² | Кровельный пирог / ввод вручную |
| γc | Коэффициент надёжности по ответственности | ГОСТ 27751 |
| Нагрузка фахверка | кг/м.п. | — |
| Тип здания | Для опор трубопроводов | — |

---

## Кровельный пирог

Вместо ввода суммарной нагрузки числом, пользователь собирает пирог из слоёв:

**Предустановленные конфигурации:**
- Унипрофиль (профнастил + минвата 150 мм + ПВХ мембрана)
- Сэндвич-панель 200 мм (готовая кровля)
- Утеплённая PIR 150 мм + наплавляемая гидроизоляция
- Холодная кровля (профнастил без утеплителя)

**Доступные слои (~30 материалов):**

| Группа | Примеры |
|---|---|
| Несущий настил | Профнастил Н-75/Н-60/НС-35 (t=0.5–0.8 мм) |
| Утеплитель минвата | 50/100/150/200 мм (ρ=80 кг/м³) |
| Утеплитель PIR | 80/100/150/200 мм (ρ=35 кг/м³) |
| Утеплитель пенополистирол | 100/150/200 мм (ρ=25 кг/м³) |
| Пароизоляция | Полиэтиленовая плёнка, фольгированная, битумная |
| Гидроизоляция | ПВХ/ТПО/ЭПДМ мембрана 1.5 мм, наплавляемая СБС 2 слоя |
| Готовые системы | Сэндвич-панель кровельная 150/200 мм |
| Прочие | Стяжка цементная, доборные элементы |

Нагрузку можно также ввести вручную, установив соответствующий флажок.

---

## Методика расчёта

### Прогоны

Нагрузка на прогон:
```
q = (Q_кровля + g_св·γf + Q_снег·γf_с + Q_пыль·γf_п + 1.0·γc_т) · a_пр · γc
```
Шаг прогонов `a_пр` задаётся в интерфейсе, по умолчанию `3.0 м`. Профиль подбирается по **Таблице 3 методики**:

| B | Профиль |
|---|---|
| 6 м | Швеллеры ГОСТ 8240 (Ш20–Ш30, 2×Ш20) |
| 12 м | Двутавры Б-серии ГОСТ 26020 (30Б1–2×30Б1) |

Если `Q_tech < 1.0 кН/м²`, программа выводит предупреждение и не меняет значение автоматически.

### Колонны

Высоты рассчитываются с учётом высоты подкрановой балки и рельса:

- **Надкрановая часть:** `H_v = H₂ − h_рельса + h_б + h_р`
- **Подкрановая часть:** `H_n = h_рельса − h_б − h_р + H_F`

Высота рельса по г/п крана:

| Г/П, т | Тип рельса | h_р, м |
|---|---|---|
| ≤ 20 | КР70 | 0.130 |
| ≤ 50 | КР80 | 0.150 |
| ≤ 80 | КР100 | 0.170 |
| > 80 | КР120 | 0.180 |

Нижняя часть колонны учитывает `Dmax`, `Gpb`, `Gwl` и `Gcu`; в отчёте `Dmax` разбивается на:

- `Dmax_qeq` - эквивалентная нагрузка от крана
- `Dmax_rail` - вес рельса
- `Dmax_beam` - вклад подкрановой балки через `αпб`

### Подкрановые балки и режим работы крана

Применяются **раздельные коэффициенты** для каждого метода:

| Метод | База таблиц | Коэф. 1К–6К | Коэф. 7К–8К |
|---|---|---|---|
| М1 (формула) | 1К–6К | 1.00 | **×1.80** |
| М2 (таблицы) | 7К–8К | ×0.65 (−64%) | **1.80** |

Коэффициент 7К–8К = **1.80** откалиброван по методике (разд. 5.2). Снижение М2 для режима 1К–6К составляет ~64% от режима 7К–8К.

### Расчётные элементы

| Конструкция | Метод 1 | Метод 2 |
|---|---|---|
| Прогоны | ✓ | — |
| Стропильные фермы (Уголки) | ✓ | — |
| Стропильные фермы (Двутавры, Молодечно) | — | ✓ |
| Связи по покрытию | — | ✓ |
| Подстропильные фермы | ✓ | ✓ |
| Подкрановые балки + тормозные | ✓ | ✓ |
| Колонны (подкрановая и надкрановая части) | ✓ | — |
| Фахверк | — | ✓ |
| Опоры трубопроводов | — | ✓ |

---

## Эстакады

### Трубопроводные (`estakada_pipe.py`, v3.2F)

9 типоразмеров (1–9 трубопроводов, Ø до 500 мм). Ключевые поля CONFIGS:

| Поле | Описание |
|---|---|
| `num` | Номер типоразмера (уникальный) |
| `max_pipes` | Максимальное количество труб |
| `max_d_mm` | Максимальный диаметр трубы, мм |
| `std_load` | Стандартная нагрузка, т/м |
| `svc_load` | Обслуживающая нагрузка, т/м |
| `kg_m` | Удельная масса конструкции, т/м |
| `pipes_low/high` | Диапазон числа труб |

### Электрокабельные и галереи (`estakada_elec.py`, v4.2F)

Два типа: `type_id=0` — эстакада без прохода, `type_id=1` — галерея с проходом. Галереи тяжелее эстакад.

---

## Тесты

```bash
python -m pytest tests/ -v
```

Покрытие: **226 тестов**, все проходят.

### `test_main_desktop.py` (~214 тестов)

| Класс | Что тестируется |
|---|---|
| `TestLkp`, `TestCeilToTable`, `TestInterpTable` | Вспомогательные функции интерполяции |
| `TestPurlinTable`, `TestSelectPurlin` | Подбор прогонов (B=6м vs B=12м) |
| `TestRoofMaterials` | Данные кровельных материалов |
| `TestTruss`, `TestSubtruss` | Масса ферм и подстропильных ферм |
| `TestBracing` | Масса связей |
| `TestCraneBeamAlpha`, `TestRailWeight`, `TestCraneModeFactor` | Данные кранового оборудования |
| `TestGetCraneBeam`, `TestGetBrake` | Выборка масс из таблиц подкрановых балок |
| `TestFakhverk`, `TestPipeSupport` | Фахверк и опоры трубопроводов |
| `TestCalculateSingleSpan` | Полный расчёт одного пролёта (12 сценариев) |
| `TestCalculateCraneMode` | Корректность коэффициентов режима (М1 ×1.80, М2 ×0.65/×1.80) |
| `TestCalculatePurlin` | Правильный профиль прогона по B |
| `TestCalculateMultiSpan` | Многопролётные здания (2–3 пролёта, смешанные режимы) |
| `TestCalculateColumnHeights` | Высоты колонн при разных УГР |

### `test_estakada.py` (~12 тестов)

| Класс | Что тестируется |
|---|---|
| `TestPipeConfigs` | Структура и физическая корректность CONFIGS трубоэстакад |
| `TestElecConfigs` | Структура и физическая корректность CONFIGS электроэстакад |

### `conftest.py`

Заглушки customtkinter и tkinter регистрируются через `sys.modules.setdefault()` до первого импорта тестируемых модулей. Это позволяет тестировать чистую расчётную логику без реального дисплея.

---

## Сборка в исполняемый файл

```bash
# macOS DMG
pyinstaller launcher.spec
# Windows EXE
pyinstaller launcher_win.spec
```

Подробнее — [docs/BUILD.md](docs/BUILD.md).

Автоматическая сборка macOS DMG и Windows EXE через GitHub Actions при push в `main`.

### iOS (iPhone/iPad)

```bash
# Локальная сборка (нужен Xcode + conda-среда kivy-ios):
./build_ios_local.sh
```

Подробнее — [build_ios_local.sh](build_ios_local.sh) и [.github/workflows/build_ios.yml](.github/workflows/build_ios.yml).

Сборка iOS (симулятор) автоматически запускается через GitHub Actions. Подписанный IPA требует настройки секретов Apple в репозитории.

---

## Структура данных `calculate()`

`calculate(gp, spans)` принимает глобальные параметры и список пролётов, где для каждого пролёта задаются:

- `L`, `B_step`, `col_step`
- `truss_type`, `q_crane_t`, `n_cranes`
- `with_passage`, `crane_mode`, `h_rail`, `H_col_ov`
- `Q_snow`, `Q_dust`, `Q_roof`, `Q_tech`, `yc`
- `fakhwerk_load`, `fakhwerk_post`, `building_type`
- `purlin_step`

Возвращает словарь с разделами расчёта по элементам: `прогоны`, `фермы`, `связи_покрытия`, `подстропильные_фермы`, `подкрановые_балки`, `колонны`, `фахверк`, `опоры_трубопроводов`, `итого`.
